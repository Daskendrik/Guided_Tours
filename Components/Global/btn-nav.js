'use strict';


function ButtonNav(prop) {}

const buttonNav = <span className="btn">{name}</span>;

const container = document.getElementById('header-root');
const root = ReactDOM.createRoot(container);
root.render(buttonNav);
